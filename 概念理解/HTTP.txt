HTTP1存在的问题, 
TCP慢启动/多条TCP连接竞争带宽和对头阻塞


HTTP2的实现原理：
在协议栈中，添加二进制分帧层来实现。头部压缩，提高文件传输速率


HTTPS 相当于在HTTP和TCP之间加了一个安全层(SSL/TLS).
    对发起HTTP的请求进行加密，对接收到的HTTP内容进行解密。
    "CA"数字证书

同源策略在 DOM、数据、网络层面给予限制。但不够灵活，
舍弃一部分安全性来换取灵活性就会遇到一些安全问题比如XSS攻击和CSRF