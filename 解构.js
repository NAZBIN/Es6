jQuery.ajax = function(url,{
	async = true,
	beforeSend = function(){},
	cache = true,
	complete = function(){},
	crossDomain = false,
	global = true
} = {}){
}

for(let val of "foo"){
	console.log(val);
}

let {name,time} = {name:"Tom",time:"todaty"};

`Hello ${name}, how are you ${time} ?`